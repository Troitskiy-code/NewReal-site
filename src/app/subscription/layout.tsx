import { PAGE_METADATA } from "@/lib/seo";

export const metadata = PAGE_METADATA.subscription;

export default function SubscriptionLayout({ children }: { children: React.ReactNode }) {
  return children;
}
