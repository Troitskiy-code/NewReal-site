import { PAGE_METADATA } from "@/lib/seo";

export const metadata = PAGE_METADATA.forgotPassword;

export default function ForgotPasswordLayout({ children }: { children: React.ReactNode }) {
  return children;
}
