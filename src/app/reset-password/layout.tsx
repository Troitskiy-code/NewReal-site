import { PAGE_METADATA } from "@/lib/seo";

export const metadata = PAGE_METADATA.resetPassword;

export default function ResetPasswordLayout({ children }: { children: React.ReactNode }) {
  return children;
}
